<h1>Editing Comment</h1>

<%= render 'form', comment: @comment, url: product_comment_path(product_id: @product.id) %>

<%= link_to 'Show', product_comment_path(@product, @comment) %> |
<%= link_to 'Back', product_comments_path %>
