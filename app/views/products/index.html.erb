<ul>
    <%= paginate @products %>
    <%- @products.each do |product| %>
        <li>
           <%= product.title %> - <%= pluralize(product.comments.count, 'commentaire') %>
           <%= link_to 'Show', product %>
        </li>
    <% end %>
</ul>